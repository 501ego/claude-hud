// Cost is rendered inline on the project line when showCost is enabled.
// This module is kept for barrel export compatibility.
import type { RenderContext } from '../../types.js';

export function renderCostLine(_ctx: RenderContext): string | null {
  return null;
}
